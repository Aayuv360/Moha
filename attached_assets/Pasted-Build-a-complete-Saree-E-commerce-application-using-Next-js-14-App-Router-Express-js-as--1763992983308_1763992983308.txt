Build a complete Saree E-commerce application using:

- Next.js 14 (App Router)
- Express.js (as backend API)
- MongoDB (Mongoose)
- Tailwind CSS for UI
- JWT authentication
- Cloudinary for image upload
- Zustand or Redux for app state

The project must include:  
--------------------------------------------------
💠 USER TYPES:
1. **Admin** – Login using Email + Password  
2. **Store Owner** – Login using Email + Password  
3. **User** – Login using Mobile Number (OTP simulation)

💠 DATABASE MODELS:
1. Admin  
2. Store  
3. User  
4. Product  
   - title  
   - description  
   - price  
   - images  
   - stockQty  
   - category (saree type)
5. Order  
   - userId  
   - items (product + qty)  
   - storeId  
   - totalAmount  
   - status (pending, shipped, delivered, cancelled)

--------------------------------------------------
💠 REQUIRED FEATURES

### **ADMIN PANEL**
- Admin login via email + password
- CRUD for Store Owners (add, delete, block)
- View all orders
- View all products

### **STORE PANEL**
- Store login via email + password
- **Page 1: Product Management**
  - Add product with image upload (Cloudinary)
  - Edit product
  - Delete product

- **Page 2: Order Management**
  - View all orders for that store
  - Update order status (pending → shipped → delivered)
  - When an order is marked shipped, reduce product stock quantity

### **USER SECTION**
- Mobile number login (OTP simulation—just 4-digit random code)
- View products
- Add to cart
- Checkout
- Place order
- Order status tracking

--------------------------------------------------
💠 API STRUCTURE (Express)
Routes:
/api/admin/login  
/api/store/login  
/api/user/login  
/api/products  
/api/orders  
/api/store/products  
/api/store/orders  

--------------------------------------------------
💠 FRONTEND (Next.js)
Pages Needed:
- /admin/login  
- /admin/dashboard  
- /store/login  
- /store/dashboard  
- /store/products  
- /store/orders  
- /products  
- /product/[id]  
- /checkout  
- /orders  

--------------------------------------------------
💠 TECH REQUIREMENTS
- Secure JWT authentication middleware
- MongoDB models with validation
- Cloudinary upload endpoint for images
- Protected routes (Admin / Store / User)
- Error handling & success toast notifications
- Responsive design

--------------------------------------------------
Generate the full application folder structure:
- backend/express server
- frontend/nextjs app
- shared config
- .env examples

Also provide:
- Installation steps
- How to run backend & frontend together
- Example .env files
